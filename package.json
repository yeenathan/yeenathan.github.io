{
  "name": "blog-ssg",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "tsx src/build.ts",
    "serve": "tsx src/serve.ts",
    "dev": "npm run build && npm run serve",
    "deploy": "rm -r dist && npm run build && git checkout -B gh-pages && cp -r dist/* . && git add posts static index.html && git commit -m \"build: $(date)\" && git push -u origin gh-pages && git checkout main"
  },
  "dependencies": {
    "markdown-it": "^14.0.0",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
